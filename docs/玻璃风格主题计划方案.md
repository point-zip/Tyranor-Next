# 玻璃风格主题计划方案

> 目标：在应用设置中新增「外观风格」下拉项（默认 / 玻璃）。玻璃风格下：页面背景固定为黑灰渐变，
> 所有卡片/条目/弹窗/面板变为半透明毛玻璃 + 0.5px 描边；外观模式与色调切换置灰不可用，
> 色调轮盘保持可用。切换即时生效、无需重启，默认风格保持现有视觉完全不变。

---

## 1. 决策记录（已与用户确认）

| 决策点 | 结论 |
|---|---|
| 毛玻璃实现 | **半透明拟态玻璃**：半透明深色玻璃面 + 0.5px 亮边；不逐组件做真采样模糊（背景是纯渐变，视觉差异极小；全 API 兼容、零 blur 管线开销）。底部导航沿用现有 `kyant backdrop` 真模糊 |
| 强调色 | **色调轮盘在玻璃风格下保持可用**，沿用用户已选主题色，不强制默认蓝 |
| 外观模式 | **玻璃风格下禁用**（背景固定黑灰渐变，浅色无意义）；切回默认风格后恢复原值 |
| 背景渐变 | 固定黑灰竖向渐变（建议 `#26282C → #0E0F11`） |
| 描边 | 统一 0.5dp 发丝线（实现见 4.4；如需物理 0.5px 可在单点调整） |
| 计划落档 | 本文档 |

## 2. 现状与改动面

- 主题入口：`theme/Theme.kt`（Material）、`theme/MiuixSettingsTheme.kt`（Miuix）双主题，均从
  `theme/AppThemeColors.kt` 快照读取 primary / isDark / toneSwitch，`@NonSkippableComposable` 保证即时重组。
- 颜色常量：`theme/Color.kt` 的 `PageGrey`（页面背景）/ `NavWhite`（卡片容器）/ `TextColor` /
  `UnselectedGrey` 均为读取快照的 `get()`，全局引用点约 17 个文件。
- 设置存储：`core/settings/AppSettingsStore.kt`（独立 `app_settings` prefs），导航栏样式
  `nav_style` 是现成参照（字符串常量 + get/set + 内存 `StateFlow`）。
- 应用设置页：`ui/settings/AppSettingsActivity.kt:70`，已有语言 / 色调轮盘 / 外观模式 / 色调切换 / 液态玻璃导航五张 MiuixCard。
- 根背景：`MainActivity.kt:65` 的 `Surface(colorScheme.background)` 与 `ui/common/AppScreen.kt:64`
  的 `AppScreenScaffold`（所有二级页宿主）是唯二的根 Surface；`MainScreen.kt:122` 内容层再铺一次背景色。
- 公共容器组件：`AppNavItem`（页面 `NavWhite` / 弹窗内 `PageGrey` 对偶）、`AppAlertDialog`
  （白底 8dp 卡片）、`AppTopBar`（页面背景色顶栏）、`AppSearchField`（Miuix SearchBar，底色取
  `surfaceContainerHigh`）、`GameActionsSheet`（`ModalBottomSheet`，底色取 `colorScheme.background`）。
- Miuix 组件取色全部来自 `MiuixTheme.colorScheme`（Card→`surfaceContainer`、输入框→
  `surfaceContainerHigh`、弹窗遮罩→`windowDimming`、分割线→`dividerLine`），在
  `MiuixSettingsTheme` 内改 scheme 即可批量生效。

## 3. 设置模型

### 3.1 `core/settings/AppSettingsStore.kt`

```kotlin
const val KEY_APPEARANCE_STYLE = "appearance_style"
const val APPEARANCE_STYLE_DEFAULT = "default"
const val APPEARANCE_STYLE_GLASS = "glass"

fun getAppearanceStyle(c: Context): String =
    prefs(c).getString(KEY_APPEARANCE_STYLE, APPEARANCE_STYLE_DEFAULT)
        ?.takeIf { it == APPEARANCE_STYLE_GLASS } ?: APPEARANCE_STYLE_DEFAULT

fun setAppearanceStyle(c: Context, style: String) { /* normalize + putString */ }
```

不新增 StateFlow：UI 统一订阅 `AppThemeColors` 快照（与色调/外观模式同源），避免双状态源。

### 3.2 `theme/AppThemeColors.kt`

```kotlin
var isGlass by mutableStateOf(false); private set

fun refresh(context: Context) {
    isGlass = AppSettingsStore.getAppearanceStyle(context) == AppSettingsStore.APPEARANCE_STYLE_GLASS
    primary = parseColorHex(AppSettingsStore.getThemeColorHex(context))   // 轮盘可用，不强制
    isDark = isGlass || AppSettingsStore.isDarkEffective(context)         // 玻璃恒深色
    toneSwitchEnabled = !isGlass && AppSettingsStore.isToneSwitchEnabled(context)
    // ThemeColorPayload：isGlass 时 darkMode=true、card/text 用玻璃面板色
}
```

- `ensureLoaded` 不动：首帧由两套主题调用 `refresh`，玻璃风格在首帧即生效。
- 设置页切换风格时：`AppSettingsStore.setAppearanceStyle(...)` → `AppThemeColors.refresh(ctx)`，
  全 App 即时重组。

## 4. 视觉体系

### 4.1 色板（新增到 `theme/Color.kt`，随 `isGlass` 取值的动态常量）

```kotlin
// 玻璃专用（仅 isGlass 分支使用）
val GlassBgTop = Color(0xFF24262B)           // 渐变起点（冷灰顶，压暗）
val GlassBgMid = Color(0xFF17191D)           // 渐变中段过渡
val GlassBgBottom = Color(0xFF0A0B0D)        // 渐变终点（近黑）
val GlassBgGlow = Color(0x0AFFFFFF)          // 顶部环境光晕（径向，4% 白）
val GlassSurface = Color(0xCC191B1F)         // 卡片/条目：80% 深色，保证可读
val GlassSurfaceHigh = Color(0xE61F2126)     // 输入框/弹窗内条目：90% 深色
val GlassPanel = Color(0xE6141518)           // 弹窗/抽屉面板：90% 深色
val GlassBorder = Color(0x24FFFFFF)          // 0.5px 描边：14% 白
val GlassText = Color(0xFFEDEEF0)            // 主文字
val GlassTextSecondary = Color(0xFFA6AAB0)   // 次要文字
val GlassUnselected = Color(0xFF7F858D)      // 未选中
```

> 背景最终形态：`GlassBackground` 用 `drawWithCache` 叠四层——三档竖向渐变
> （0f 冷灰顶 / 0.52f 中灰 / 1f 近黑）+ 顶部白色环境光（圆心略高于屏幕顶端、半径约 55% 屏高、4% 白）
> + 主题色环境光对角两层（右上主：alpha 0.26、中心 0.85w/0.22h、半径 1.05w；左下辅：alpha 0.12、
> 中心 0.10w/0.78h）。主题色环境光随「色调轮盘」实时变化，让玻璃面有颜色可透。
>
> 面板透明度取舍：初稿的 8%/12% 白在纯渐变上会过度透明（下拉浮层、搜索框无额外底板），
> 最终取 80%~90% 深色半透明，保留渐变透视的同时保证可读。

`PageGrey / NavWhite / TextColor / UnselectedGrey` 增加 `isGlass` 分支：

| 常量 | 默认风格 | 玻璃风格 |
|---|---|---|
| `PageGrey` | 现状 | `Color.Transparent`（露出根部渐变） |
| `NavWhite` | 现状 | `GlassSurface` |
| `TextColor` | 现状 | `GlassText` |
| `UnselectedGrey` | 现状 | `GlassUnselected` |

新增对偶常量 `DialogItemSurface`：默认 = `PageGrey`；玻璃 = `GlassSurfaceHigh`。
用于「弹窗内条目」——玻璃下 `PageGrey` 透明会导致条目在玻璃弹窗上不可见，原
`AppNavItem(containerColor = PageGrey)` 的弹窗调用点改传它。

### 4.2 两套主题的 scheme 映射

`theme/Theme.kt`（Material）：`background/surface = PageGrey`（玻璃下透明）、
`onBackground/onSurface = TextColor` 已具备；补充 `surfaceVariant/outline` 指向玻璃色即可。

`theme/MiuixSettingsTheme.kt`（玻璃分支）：

```kotlin
background = PageGrey                // 透明，露出渐变
surface = PageGrey
surfaceContainer = GlassSurface      // MiuixCard 默认底色 → 全部卡片自动玻璃化
surfaceContainerHigh = GlassSurfaceHigh       // SearchBar/InputField
surfaceContainerHighest = GlassSurfaceHigh
onBackground = onSurface = onSurfaceContainer = TextColor
onSurfaceContainerVariant = GlassTextSecondary
outline = dividerLine = GlassBorder  // 下拉浮层、分割线
windowDimming = Color.Black.copy(alpha = 0.6f)
primary = 存储主题色（轮盘可用）
```

### 4.3 渐变根背景（新增 `theme/GlassStyle.kt`）

```kotlin
@Composable
fun GlassBackground(modifier: Modifier = Modifier, content: @Composable () -> Unit) {
    val modifier = if (AppThemeColors.isGlass)
        modifier.background(Brush.verticalGradient(listOf(GlassBgTop, GlassBgBottom)))
    else modifier else modifier.background(MaterialTheme.colorScheme.background)
    Box(modifier.fillMaxSize()) { content() }
}
```

落地两处根：
- `MainActivity.kt:65`：`Surface` 替换为 `GlassBackground { MainNavigation() }`。
- `ui/common/AppScreen.kt:70`：`Surface` 替换为 `GlassBackground { ... }`（覆盖全部二级页）。
- `MainScreen.kt:122` 保持 `.background(colorScheme.background)`（玻璃下透明），不重复铺渐变。

### 4.4 玻璃描边修饰符（`theme/GlassStyle.kt`）

```kotlin
private val GlassShape = RoundedCornerShape(8.dp)

fun Modifier.glassBorder(
    shape: Shape = GlassShape,
    width: Dp = 0.5.dp,
    color: Color = GlassBorder,
): Modifier = if (AppThemeColors.isGlass)
    border(BorderStroke(width, color), shape) else this
```

- 读取快照 state → 风格切换自动重组。
- 0.5dp 在高密度屏渲染为 1~2 物理像素；若要求物理 0.5px，可单点改为
  `drawWithCache { onDrawWithContent { drawContent(); drawOutline(...) } }` 用 `0.5f` 像素，
  本方案默认 0.5dp（视觉发丝线）。
- MiuixCard 用 squircle 填充、描边用 RoundedCornerShape(8dp)，8dp 半径下差异肉眼不可辨，接受。

## 5. 组件改造清单

### 5.1 公共组件（改组件内部，调用方零改动）

| 组件 | 改动 |
|---|---|
| `AppNavItem` | 玻璃下 `NavWhite` 已自动变玻璃面；在 `.background(containerColor)` 后追加 `.glassBorder()`（容器传 `PageGrey` 透明时不加边） |
| `AppAlertDialog` | 卡片色 `NavWhite` → 玻璃下改用 `GlassPanel`；追加 `.glassBorder()`；遮罩透明度保持 |
| `AppTopBar` | 玻璃下背景透明（`PageGrey`/`MiuixTheme.background` 自动透明），文字 `TextColor`；可选加底部 0.5px 分隔线（用 `glassBorder` 只画下边） |
| `AppSearchField` | Miuix `surfaceContainerHigh` 覆盖后自动玻璃化；外层 `.glassBorder(RoundedCornerShape(8.dp))` |
| `GameActionsSheet` | `containerColor` 玻璃下改 `GlassPanel`；顶部圆角 `glassBorder`；条目仍 `NavWhite`（AGENT.md 抽屉条目约定） |
| `LiquidGlassNavigationBar` | 不改；玻璃风格下 `isDark=true`，其表面色自动走深色分支 |
| `BottomInsetSpacer` | 不改 |

### 5.2 页面容器（补描边即可，颜色多数自动跟随）

- Material `Card`（`EngineScreen EngineRow`、`SaveManagement` 卡片、`PatchEntryCard`、
  `GameShortcutCropDialog` 卡片、`CoverSearchDialog` 卡片）：`containerColor = NavWhite` 自动玻璃化，
  逐处追加 `.glassBorder(RoundedCornerShape(8.dp))`。
- `GameCard`（`GameScreen.kt:1466`）：卡片自身是封面/引擎色占位，追加玻璃描边。
- `HomeScreen`：`QuickLaunchEmptyCard`、`RecentGameRow` 内容层、`QuickLaunchCard` 追加描边；
  `QuickLaunchFallback` 固定深灰保持。
- MiuixCard 调用点（`SettingsScreen`、`AppSettingsActivity`、`PerGameSettingsScreen`、
  `EngineSettingsMenuActivity`、`CoverScraperSettingsActivity`、`RpgMakerSettingsCard`、
  `FontSettings`、`EngineSettingsDetailScreen` 等约 20 处）：颜色走 scheme 自动玻璃化，
  逐处 `modifier = Modifier.fillMaxWidth().glassBorder()`。
  （可选：封装 `GlassCard` 包装组件一次性收口，首版按最小改动逐处加。）
- 弹窗内条目 `containerColor = PageGrey` 的调用点（`EngineScreen.kt:129`、`RpgMakerSettingsCard.kt:327/334`、
  `FontSettings.kt:101/109`、`SettingsScreen.kt:507/511/549/569` 等）改传 `DialogItemSurface`。
- 页面内非卡片式 `PageGrey` 用法（`CoverScraperSettingsActivity.kt:256` 序号徽标、
  `GameScreen.kt:1251/1389`、`SettingsScreen.kt:389`）逐处判断：玻璃下透明是否可读，不可读改 `DialogItemSurface`。

## 6. 设置页改造（`ui/settings/AppSettingsActivity.kt`）

1. 新增一张 MiuixCard + `OverlayDropdownPreference`（建议放「外观模式」之前）：

```kotlin
val appearance = if (AppThemeColors.isGlass)
    AppSettingsStore.APPEARANCE_STYLE_GLASS else AppSettingsStore.APPEARANCE_STYLE_DEFAULT
OverlayDropdownPreference(
    title = stringResource(R.string.settings_appearance_style),
    items = listOf(
        stringResource(R.string.settings_appearance_style_default),
        stringResource(R.string.settings_appearance_style_glass),
    ),
    selectedIndex = if (appearance == AppSettingsStore.APPEARANCE_STYLE_GLASS) 1 else 0,
    onSelectedIndexChange = { index ->
        AppSettingsStore.setAppearanceStyle(ctx, if (index == 1) GLASS else DEFAULT)
        AppThemeColors.refresh(ctx)
    },
)
```

2. 禁用项（玻璃风格时置灰，`enabled` 参数 Miuix 0.9.2 已支持）：
   - 外观模式 `OverlayDropdownPreference(enabled = !isGlass)`；
   - 色调切换 `SwitchPreference(enabled = !isGlass)`；
   - 色调轮盘 `ArrowPreference` 保持可用（本次确认）。
   - 建议给两张禁用卡加 `summary = stringResource(R.string.settings_disabled_in_glass_style)` 说明原因。
3. 三语言新增 key（`values/values-ja/values-en`，跑 `checkHardcodedUiStrings`）：

| key | zh | ja | en |
|---|---|---|---|
| `settings_appearance_style` | 外观风格 | 外観スタイル | Appearance style |
| `settings_appearance_style_default` | 默认 | デフォルト | Default |
| `settings_appearance_style_glass` | 玻璃 | ガラス | Glass |
| `settings_disabled_in_glass_style` | 玻璃风格下不可用 | ガラススタイルでは使用できません | Unavailable in Glass style |

## 7. 系统栏与引擎壳联动

- `ui/common/AppScreen.kt:30`：`val darkMode = AppSettingsStore.isDarkEffective(this)` 追加
  `|| getAppearanceStyle(this) == APPEARANCE_STYLE_GLASS`，玻璃下用浅色系统栏图标
  （`SystemBarStyle.dark`）。
- `MainActivity.kt:51` 的 `SideEffect` 读 `AppThemeColors.isDark`，玻璃下自动为 true，无需改。
- `AppThemeColors.refresh` 的 `ThemeColorPayloadStore`：玻璃下 `darkMode=true`、
  card/text/muted 用 `GlassPanel/GlassText/GlassTextSecondary` 的 ARGB，保证 Tyrano/Web 壳配色一致。

## 8. 降级、性能与边界

- **API < 31**：不依赖 RenderEffect，半透明 + 描边完整生效，无模糊损失；`LiquidGlassNavigationBar`
  继续走其 <31 实色分支。
- **性能**：全 App 只多一层渐变 Box 与每组件一条 hairline；无逐组件 blur 管线，滚动/网格无新增开销。
- **无法真采样处（弹窗/抽屉是独立窗口）**：统一用 `GlassPanel` 高不透明度面板，可读性优先。
- **对比度**：文字统一 `GlassText`；封面网格上文字仍在卡片内部；若个别页面出现低对比，单点调 `GlassPanel` alpha。
- **默认风格零回归**：所有玻璃逻辑均由 `isGlass` 门控，`isGlass=false` 时行为与现状逐像素一致。

## 9. 实施步骤

1. `AppSettingsStore`：新增 `appearance_style` 常量与 get/set。
2. `AppThemeColors`：`isGlass` 快照 + `refresh` 分支 + ThemeColorPayload 映射。
3. `Color.kt`：玻璃色板 + 四个动态常量玻璃分支 + `DialogItemSurface`；`Theme.kt`/`MiuixSettingsTheme.kt` scheme 映射。
4. 新增 `theme/GlassStyle.kt`：`GlassBackground` + `Modifier.glassBorder`。
5. 根背景接入：`MainActivity`、`AppScreenScaffold`；`AppScreenActivity` 系统栏判定。
6. 公共组件：`AppNavItem` / `AppAlertDialog` / `AppTopBar` / `AppSearchField` / `GameActionsSheet`。
7. 页面容器逐处补描边 + 弹窗内条目改 `DialogItemSurface`（第 5 节清单）。
8. 设置页：新增外观风格下拉、禁用外观模式与色调切换、三语言文案。
9. 验证：`./gradlew :app:assembleDebug`（含 `checkHardcodedUiStrings`），
   手动回归矩阵：默认/玻璃 × 四 Tab × 二级页 × 弹窗 × 浅色/深色存量值 × API 30/31+ 模拟器。

## 10. 验收标准

- [ ] 设置 → 应用设置出现「外观风格=默认/玻璃」，切换即时生效且重启保持。
- [ ] 玻璃下：黑灰渐变背景、全部卡片/条目/弹窗/抽屉/搜索框为毛玻璃观感且带 0.5px 描边。
- [ ] 玻璃下：外观模式与色调切换置灰不可点；色调轮盘可打开并即时改变强调色。
- [ ] 玻璃下状态栏/导航栏图标为浅色；引擎网页壳跟随深色。
- [ ] 切回默认：视觉与改动前一致（抽查首页/游戏/设置/引擎四页与弹窗）。
- [ ] 三语言字符串齐全、`checkHardcodedUiStrings` 通过、`assembleDebug` 通过。

## 11. 风险与回滚

| 风险 | 缓解 |
|---|---|
| `colorScheme.background` 透明导致某处露出窗口透明 | 根部渐变全覆盖（MainActivity/AppScreenScaffold）；验收时专项检查对话框/输入法场景 |
| 弹窗内条目原先靠 `PageGrey` 与白底反差，玻璃下失效 | 新增 `DialogItemSurface` 统一替换，grep 全部 `PageGrey` 调用点逐个判定 |
| Miuix squircle 与 RoundedCornerShape 描边错位 | 8dp 小半径不可辨；如出现可见偏差，描边改由 `squircleBackground` 叠加或降低描边 alpha |
| 个别存量主题色（低饱和）在玻璃下对比不足 | 轮盘保持可用可自行调整；不做强制替换 |
| 回滚 | 设置仅多一个 prefs key；代码改动全部 `isGlass` 门控，回滚即删除玻璃分支或把默认值锁回 `default` |

## 12. 实施记录

已按本方案落地，主要改动：

| 文件 | 改动 |
|---|---|
| `core/settings/AppSettingsStore.kt` | 新增 `KEY_APPEARANCE_STYLE`、`APPEARANCE_STYLE_DEFAULT/GLASS` 与 get/set（非法值归一为默认） |
| `theme/AppThemeColors.kt` | 新增 `isGlass` 快照；`refresh` 计算玻璃/深色/色调切换；ThemeColorPayload 玻璃分支 |
| `theme/Color.kt` | 玻璃色板 + `PageGrey/NavWhite/TextColor/UnselectedGrey` 玻璃分支 + `DialogItemSurface` |
| `theme/MiuixSettingsTheme.kt` | 玻璃下 `surfaceContainerHigh/Highest`、`secondaryContainer`、outline/divider、windowDimming 等映射 |
| `theme/GlassStyle.kt`（新增） | `GlassBackground`（渐变根背景）+ `Modifier.glassBorder`（drawWithContent 在内容后画 0.5dp 描边） |
| `MainActivity.kt` / `ui/common/AppScreen.kt` | 根 Surface 换成 `GlassBackground`；AppScreenActivity 系统栏判定含玻璃，并在页面内实时跟随 |
| `ui/common/AppNavItem/AppAlertDialog/AppSearchField` | 条目/弹窗/搜索框玻璃化（描边、面板色、胶囊形描边） |
| `ui/game/GameScreen.kt` | 操作抽屉、封面搜索弹窗用 `GlassPanel`；候选占位/启动文件行改 `DialogItemSurface`；GameCard 描边 |
| `ui/home/HomeScreen.kt` | 快捷启动卡/空卡/最近打开行描边 |
| `ui/engine`、`ui/save`、`ui/patch`、`ui/cover`、`ui/game/GameShortcutCropDialog` | 页面卡片描边、弹窗内条目改 `DialogItemSurface` |
| `ui/settings/*`（含 RpgMaker/FontSettings/PerGame/EngineSettingsMenu） | 全部 MiuixCard 描边、弹窗条目改 `DialogItemSurface` |
| `ui/settings/AppSettingsActivity.kt` | 新增「外观风格」下拉卡；玻璃下外观模式与色调切换 `enabled=false` 并显示原因；色调轮盘保持可用 |
| `res/values{,-ja,-en}/strings.xml` | 新增 4 个 key |
| `AGENT.md` / `CONTEXT.md` | 同步玻璃风格容器色规范与「外观风格」术语 |

验证：`:app:compileDebugKotlin`、`:app:testDebugUnitTest`、`:app:checkHardcodedUiStrings` 全部通过
（其余为需真机的视觉验收项，见第 10 节）。

### 12.1 实机验收后的修正

| 问题 | 原因 | 修正 |
|---|---|---|
| 操作抽屉（GameActionsSheet）背景仍显透明 | 面板 `GlassPanel`(90%) 与条目 `NavWhite`(80%) 都带 alpha，底层亮色封面会透出 | `Color.kt` 新增不透明 `GlassPanelSolid`/`GlassSurfaceSolid`；抽屉面板与 11 处条目全部改不透明；玻璃下 scrim 加深至 60% |
| 抽屉挂外层 `glassBorder` 后出现白线/把手贴左 | ① 抽屉的垂直位置由内部 `draggableAnchors` 布局偏移决定，外层描边按未偏移坐标绘制，白线落进背景；② 自定义 `dragHandle` 的 `Column` 默认水平 Start 对齐把把手挤到左边 | 移除抽屉外层描边；改为在 `dragHandle` 槽首位画 0.5dp 顶边线，`Column` 设 `fillMaxWidth + CenterHorizontally` |
| 首页最近游戏侧滑删除层泛主题色半透明 | 删除层用 `colorScheme.primary`，半透明条目压在其上透色 | 玻璃下删除层固定用亮玻璃面 `GlassSurfaceHigh`；默认风格保持主题色 |
| `AppSearchField` 描边圈到外层布局 | 描边挂在 Miuix `SearchBar` 的 `modifier`（外层 Column，含 insideMargin 等），而胶囊底色画在 `InputField` 内 | 描边移到 `InputField` 的 `modifier`，形状用 `CircleShape` 与胶囊底色一致 |
| 页面背景渐变观感发闷 | 单段两点竖向渐变（#26282C→#0E0F11）缺少层次 | 改为三档渐变（#24262B/#17191D/#0A0B0D）+ 顶部 4% 白径向环境光晕（`GlassBgGlow`），`drawWithCache` 一次缓存两层 Brush |
| 玻璃风格下默认导航栏仍是平底实色条 | 默认样式 `NavigationBar` 占布局高度、容器色为不透明玻璃面且无描边 | 玻璃外观风格 + 默认导航样式时，导航栏改为悬浮圆角玻璃条（`MainScreen.floatingDefaultNav`）：圆角与组件统一（`AppComponentShape`）、`glassBorder` 0.5dp 描边、`GlassNavSurface`（95% 深色）玻璃底，且只显示图标不显示文字（栏高收窄至 64dp）；`windowInsets=0` 由外层避让，列表底部留白由 `glassNavBottomInset()` 统一改为 88dp |
| 组件圆角散落字面量、玻璃下无法统一调整 | 22 处 `RoundedCornerShape(8.dp)` + 15 处 Miuix `cornerRadius = 8.dp` 硬编码 | 新增 `theme/AppShapes.kt` 统一入口：`AppComponentShape`（默认 8dp / 玻璃 32dp，与悬浮导航一致）、`AppComponentCornerRadius`（Miuix Dp）、`AppSheetTopShape`（抽屉顶部）；全项目替换为常量并清理多余 import |
| 部分页面顶栏滚动时文字与标题重叠 | 玻璃下页面背景 `PageGrey` 透明，而 MiuixScaffold 等页面的内容会从顶栏下方滚过，透明顶栏挡不住滚过的内容 | 改为「透明顶栏 + 内容垫底」：`AppTopBar` 在玻璃下保持透明（露出渐变/环境光）；6 个 MiuixScaffold 页面把 `innerPadding` 顶部值从滚动 `contentPadding` 移到列表 `modifier` 的持久 padding，内容整体垫在顶栏下方，滚动不再穿过顶栏 |
| 玻璃叠在中性黑灰渐变上「没东西可透」 | 背景缺少颜色层次，组件再透明也没有可透出的内容 | C 档背景配合：`GlassBackground` 叠加主题色对角环境光（右上主 alpha 0.26、中心 0.85w/0.22h、半径 1.05w；左下辅 alpha 0.12），随「色调轮盘」实时变色；保留 4% 白顶部光晕。实测玻璃面/间隙出现可感知的色调层次（+5~+20 蓝色通道差） |

以上均已实机验证：抽屉不透明且把手居中、无杂线；搜索框描边贴合胶囊。
