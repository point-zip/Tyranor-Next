<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- 应用内下载更新后调起系统安装器所需（canRequestPackageInstalls 判断依据） -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <queries>
        <package android:name="cyou.joiplay.runtime.rpgmaker" />
        <package android:name="cyou.joiplay.runtime.renpy.v8d4d1" />
        <package android:name="cyou.joiplay.runtime.renpy.v7d7d1" />
        <!-- 外置主机模拟器跳转目标（安装探测 + 显式组件启动） -->
        <package android:name="org.ppsspp.ppsspp" />
        <package android:name="dev.eden.eden_emulator" />
        <!-- YU-RIS 经外置 Winlator（winlator-cn 外置启动协议；应用 id 为 com.tyranor.next.win） -->
        <package android:name="com.tyranor.next.win" />
    </queries>

    <application
        android:name=".TyranorNextApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.TyranorNext">
        <!-- WorkManager 首次切入后台时再按需初始化，避免进入前台时占用冷启动关键路径。 -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- 更新弹窗内下载的 APK 通过 FileProvider 共享给系统安装器 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.save.SaveManagementActivity"
            android:exported="false" />
        <!-- 桌面快捷方式回跳蹦床：exported=true 为桌面 Launcher 跨进程显式启动所必需，勿改回 false（见 GameShortcutActivity KDoc） -->
        <activity
            android:name=".ui.game.GameShortcutActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.TyranorNext.Translucent" />
        <activity
            android:name=".ui.patch.KrkrOnlinePatchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.settings.EngineSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.settings.EngineSettingsMenuActivity"
            android:exported="false" />
        <activity
            android:name=".ui.settings.PerGameSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.settings.AppSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.cover.CoverScraperSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.HikarinagiOAuthCallbackActivity"
            android:exported="false"
            android:theme="@style/Theme.TyranorNext.Translucent" />
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="oauth"
                    android:path="/hikarinagi"
                    android:scheme="tyranornext" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
